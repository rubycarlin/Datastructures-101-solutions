import os

class heap:
    

def constructHeap():
    n = int(raw_input("Enter the number of nodes : "))
    for i in xrange(n):
        ele = int(raw_input("Enter the element : "))
        
        
constructHeap()
